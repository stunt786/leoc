# LEOC Environment Configuration
# PRODUCTION DEPLOYMENT CHECKLIST:
# 1. Generate a strong SECRET_KEY: python -c 'import secrets; print(secrets.token_hex(32))'
# 2. Copy the output and paste it below
# 3. Generate a strong UNLOCK_KEY and hash it: python -c 'from werkzeug.security import generate_password_hash; print(generate_password_hash("your-strong-password"))'
# 4. Set FLASK_DEBUG=False in production

# Flask settings
FLASK_APP=app.py
FLASK_ENV=production
FLASK_DEBUG=False

# CRITICAL: Set this to a strong random key in production!
# Generate with: python -c 'import secrets; print(secrets.token_hex(32))'
# DO NOT commit the actual key to version control - use your deployment system to set this
SECRET_KEY=CHANGE_THIS_TO_A_STRONG_RANDOM_KEY_IN_PRODUCTION

# Database settings
# SQLite path relative to app root or absolute path
SQLALCHEMY_DATABASE_URI=sqlite:////home/prakash/Documents/App Development/leoc/instance/leoc.db

# File upload settings
UPLOAD_FOLDER=static/uploads
MAX_CONTENT_LENGTH=16777216  # 16MB in bytes

# Server settings
PORT=5002

# CRITICAL: Change this unlock key in production to something strong and hash it with bcrypt/argon2
# Use: from werkzeug.security import generate_password_hash; print(generate_password_hash('your-password'))
# DO NOT use admin123 in production
UNLOCK_KEY=admin123

# Cache timeout in seconds
CACHE_TIMEOUT=300
