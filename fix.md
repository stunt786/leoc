Fix and update the project functionality.
**In 'Supplier' page form there is category for supplier type but in 'Stock Receipt' form the supplier type dropdown has dofferent values and there is no linkage between 'supplier' page source type & 'Stock Receipt' form source type.
**Stock Receipt Print from, add received By and Verified By below the table as signature part.
**Categories: Why predefined categories are deleting while others that have no link also throwing error, This operation failed because the record is linked to other records. Please remove all related records and try again. How predefined categories are created...
**In Inventory page add a filter option for 'Suppliers' for filtering suppliers based Items and Add From Date to To date (BS Date) to filter items received in different dates.
**Wrong calculation for 'closing stocks' number of items that are transferred from one Warehouse to another. For example if 20kg rice from 1st warehouse is transferred to 2nd warehouse then first warehouse 'stockbook' print displaying 40 as current/closing remaining stock instead of 0.
**In stock transfer page, if we select from and to warehouse to transfer stock items, the item list is displaying all items instead of available items in the selected 'from warehouse'
** In 'Items' page for 'History' feature, how 'transferred' card is calculating data, if same number of item is transferred from one warehouse to another back and forth several times, transferred shows increased numberd of transferred. For e.g if 10 kg rice is transferred from warehouse 1 to warehouse 2, then total transfer shows 20 though amount is 10 only warehouse is changed and if again transferred back to warehouse 1, it becomes 30. Is it correct calculation?
**Stock Receipt Edit: has dict.get() takes no keyword arguments error and if the stocks are transferred from one warehouse to another and tried to edit 'Stock receipt' then how it handles edit. currently it is throwing errordict.get() takes no keyword arguments. Make the best validation approach for such different cacses for edit.
**Relief Request: Showung 'Completed' status if relief request items are dispatched and not yet distributed. It should not mark complete until distribution is complete.
**Relief Request, Dispatch and Distribution calculaton: If The relief request is low e.g 5kg and if dispatch is done 8 kg, and distributed is done 4kg, then the actual distribution is 4kg only but the system is calculating 8kg and updating as 8 kg inreference tables. Add proper validation for such cases and what actually happens if less is distributed thendispatch, where the item is listed for remaining that is stuck in the middle between relief request, dispatch and distribution. Find prper way to resolve this